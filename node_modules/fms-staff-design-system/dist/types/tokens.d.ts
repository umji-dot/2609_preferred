/**
 * 공통 값 유니온.
 *
 * 컴포넌트가 선택적으로 사용합니다. variant 같은 컴포넌트별 고유 값은
 * 여기에 두지 않고 각 컴포넌트의 types 파일에서 정의합니다.
 *
 * 값은 staff 디자인 토큰(`src/styles/tokens.css`)과 정합되도록 정의합니다.
 */
/** 컴포넌트 사이즈 스케일 (staff: sm/md/lg). */
export type Size = 'sm' | 'md' | 'lg';
/** Semantic tone (staff status 토큰 4종 + neutral). */
export type Tone = 'neutral' | 'info' | 'success' | 'warning' | 'danger';
/**
 * Spacing 토큰 키 (= 픽셀값).
 * tokens.css 의 `--sp-*` (8pt grid, Figma 7개) 정의와 1:1 매칭됩니다.
 */
export type SpacingToken = 4 | 8 | 12 | 16 | 24 | 32 | 48;
//# sourceMappingURL=tokens.d.ts.map