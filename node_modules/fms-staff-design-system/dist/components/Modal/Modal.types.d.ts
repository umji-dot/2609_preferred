import { ReactNode } from 'react';
export interface ModalProps {
    /** 열림 상태 */
    open: boolean;
    /** 닫기 핸들러 */
    onClose: () => void;
    /** 모달 제목 */
    title: string;
    /** 제목 옆 추가 요소 (배지 등) */
    headerExtra?: React.ReactNode;
    /** 설명 텍스트 */
    description?: string;
    /** 모달 크기 */
    size?: 'sm' | 'md' | 'lg' | 'xl';
    /** Primary 버튼 스타일 */
    variant?: 'default' | 'danger';
    /** 로딩 상태 (버튼 disabled + spinner) */
    loading?: boolean;
    /** 에러 메시지 (상단 banner) */
    error?: string;
    /** Primary 버튼 라벨 */
    primaryLabel?: string;
    /** Primary 액션 */
    onPrimary?: () => void;
    /** Secondary 버튼 라벨 */
    secondaryLabel?: string;
    /** Secondary 액션 */
    onSecondary?: () => void;
    /** 배경/ESC 닫기 방지 */
    preventClose?: boolean;
    /** 배경(backdrop) 클릭으로 닫기 허용 여부. 기본 false — 실수 클릭 시 입력 유실 방지(닫기는 X 버튼). */
    closeOnBackdrop?: boolean;
    /** body 기본 패딩(p-6) 제거 — 임베드/캔버스형 컨텐츠용 */
    disablePadding?: boolean;
    /** 커스텀 footer (primaryLabel 대신 사용) */
    footer?: React.ReactNode;
    /** 컨텐츠 */
    children: React.ReactNode;
    className?: string;
    /** E2E 테스트 ID — dialog 노드에 부착. */
    'data-testid'?: string;
    /** E2E 테스트 ID — 닫기(X) 버튼에 부착. */
    'data-testid-close'?: string;
}
/**
 * imperative `modal.open()` 옵션.
 *
 * 선언형 `ModalProps` 에서 `open`/`onClose`/`children` 을 제외한 나머지(title/size/variant/
 * primaryLabel/onPrimary/secondaryLabel/onSecondary/description/error/footer/data-testid ...)를
 * 그대로 받고, 본문은 `children` 대신 `content` 로 전달한다.
 */
export interface ModalOpenOptions extends Omit<ModalProps, 'open' | 'onClose' | 'children'> {
    /** 모달 본문 — 선언형 `children` 에 대응 */
    content?: ReactNode;
    /** 닫힐 때(어떤 사유로든) 호출되는 콜백. confirm/alert resolve 연동용. */
    onClose?: () => void;
}
/** 스택에 쌓이는 모달 항목 — 고유 id + open 옵션. */
export interface ModalEntry {
    id: string;
    options: ModalOpenOptions;
}
//# sourceMappingURL=Modal.types.d.ts.map