/**
 * Figma Source: 03_Components > Pagination
 * props 는 staff Pagination 과 동일하게 유지 → drop-in 대체.
 */
export interface PaginationProps {
    /** 현재 페이지 (1-based) */
    currentPage: number;
    /** 전체 페이지 수 */
    totalPages: number;
    /** 페이지 변경 핸들러 */
    onChange: (page: number) => void;
    /** 크기 */
    size?: 'sm' | 'default';
    /** 전체 아이템 수 (표시용) */
    totalItems?: number;
    /** 페이지당 아이템 수 */
    pageSize?: number;
    /** 페이지 크기 옵션 */
    pageSizeOptions?: number[];
    /** 페이지 크기 변경 핸들러 */
    onPageSizeChange?: (size: number) => void;
    /** 첫/마지막 페이지 버튼 */
    showFirstLast?: boolean;
    /** 최대 페이지 버튼 수 */
    maxButtons?: number;
    /** 비활성화 */
    disabled?: boolean;
    /** 이전/다음 배치 — split(기본, 숫자 양옆) | end(숫자 왼쪽, 이전·다음 오른쪽 묶음) */
    navPlacement?: 'split' | 'end';
    className?: string;
}
//# sourceMappingURL=Pagination.types.d.ts.map