import { HTMLAttributes, ReactNode } from 'react';
/**
 * Figma Source: 03_Components > Badge
 * 컴포넌트명은 Figma 기준(Badge). status 값은 개발 컨벤션(English 키, 토큰명과 일치).
 *
 * - domain status(pending/assigned/completed/cancelled/unable): Figma 정본.
 *   라벨(대기/배정완료/완료/취소/작업불가)은 status 에 따라 내부 매핑되어 렌더된다.
 * - semantic status(success/warning/info/error/neutral): staff StatusBadge 호환 확장(Figma 외).
 *   범용 의미색 배지. children 라벨 필수.
 */
export type BadgeStatus = 'pending' | 'assigned' | 'completed' | 'cancelled' | 'unable';
export type SemanticStatus = 'success' | 'warning' | 'info' | 'error' | 'neutral';
export interface BadgeProps extends Omit<HTMLAttributes<HTMLSpanElement>, 'children'> {
    /** 상태 — domain(Figma) 또는 semantic(확장) */
    status: BadgeStatus | SemanticStatus;
    /** 라벨. 생략 시 domain 은 내부 한글 라벨, semantic 은 빈 값 */
    children?: ReactNode;
    /** 크기 (semantic/확장용). domain 기본은 Figma 고정(md) */
    size?: 'sm' | 'md' | 'lg';
    /** 좌측 아이콘 (dot 대신) */
    icon?: ReactNode;
    /** dot 표시 여부 (기본: domain=true, semantic=false) */
    showDot?: boolean;
    /** 커스텀 배경색 */
    backgroundColor?: string;
    /** 커스텀 텍스트색 */
    textColor?: string;
    /** 커스텀 dot 색 */
    dotColor?: string;
}
//# sourceMappingURL=Badge.types.d.ts.map