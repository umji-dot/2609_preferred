import { ReactNode } from 'react';
/**
 * Figma Source: 03_Components > Tabs (Tabs-A 48px=line / Tabs-B 40px=contained)
 * props 는 staff Tabs 와 동일하게 유지 → drop-in 대체.
 */
export interface TabItem {
    key: string;
    label: string;
    icon?: ReactNode;
    badge?: string | number;
    disabled?: boolean;
}
export interface TabsProps {
    items: TabItem[];
    activeKey: string;
    onChange: (key: string) => void;
    /** line(밑줄, 48px) | contained(세그먼트 카드, 40px) */
    variant?: 'line' | 'contained';
    className?: string;
    'data-testid'?: string;
}
//# sourceMappingURL=Tabs.types.d.ts.map