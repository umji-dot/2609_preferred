import { InputHTMLAttributes, ReactNode } from 'react';
/**
 * Figma Source: 03_Components > Search
 * staff 에 대응 컴포넌트 없음 → InputField props 관례를 따른다.
 * States(Figma): Default · Hover · Focus · Filled (우측 search 아이콘 18px 내장)
 */
export interface SearchProps extends Omit<InputHTMLAttributes<HTMLInputElement>, 'size' | 'onChange'> {
    /** Field label (있으면 상단 표시, 13px Regular gray-600) */
    label?: ReactNode;
    disabled?: boolean;
    fullWidth?: boolean;
    onChange?: (value: string) => void;
}
//# sourceMappingURL=Search.types.d.ts.map