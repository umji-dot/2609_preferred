import { ButtonHTMLAttributes, ReactNode } from 'react';
/**
 * Figma Source: 03_Components > Button (Button 30 / Button 36)
 *
 * Variants: primary | secondary | ghost | danger | etc
 * - primary   → bg primary(#263949), text white, border 없음
 * - secondary → bg white, border border(#e4e4e7), text gray-600
 * - ghost     → 배경·테두리 없음(투명), text gray-600
 * - danger    → bg error(#dc2626), text white (확인 모달 필수)
 * - etc       → bg nav-accent(#3ac8a9), text white
 *
 * States(Figma): Default · Hover · Active · Disabled · Loading · LeadingIcon · IconOnly
 *   → Hover/Active 는 CSS pseudo, Disabled/Loading/Icon 은 props 로 표현
 */
export type ButtonVariant = 'primary' | 'secondary' | 'ghost' | 'danger' | 'etc';
/** Figma 정의: sm(30px, 12px) / md(36px, 14px). */
export type ButtonSize = 'sm' | 'md';
export interface ButtonProps extends Omit<ButtonHTMLAttributes<HTMLButtonElement>, 'children'> {
    variant?: ButtonVariant;
    size?: ButtonSize;
    disabled?: boolean;
    loading?: boolean;
    fullWidth?: boolean;
    /** 아이콘 전용(정사각) 버튼. 텍스트 없이 leadingIcon(또는 children)만 중앙 렌더. aria-label 필수. */
    iconOnly?: boolean;
    leadingIcon?: ReactNode;
    trailingIcon?: ReactNode;
    /** iconOnly=true 면 생략 가능 */
    children?: ReactNode;
}
//# sourceMappingURL=Button.types.d.ts.map