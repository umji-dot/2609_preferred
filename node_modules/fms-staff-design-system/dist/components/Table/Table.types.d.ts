import { ReactNode } from 'react';
/**
 * Figma Source: 03_Components > DataTable
 * props 는 staff Table 과 동일하게 유지(drop-in) + Figma 합성 기능(툴바/빈상태) 추가.
 * export 는 Table(staff 호환) + DataTable 별칭(Figma 이름).
 */
export interface Column<T = any> {
    /** 컬럼 고유 ID */
    id: string;
    /** 헤더 텍스트 */
    header: string;
    /** 데이터 접근 키 */
    accessorKey?: keyof T;
    /** 커스텀 셀 렌더 */
    cell?: (row: T, index: number) => ReactNode;
    /** 정렬 가능 여부 */
    sortable?: boolean;
    /** 정렬 (default: center) */
    align?: 'left' | 'center' | 'right';
    /** 컬럼 너비 */
    width?: string | number;
}
export interface TableProps<T = any> {
    columns: Column<T>[];
    data: T[];
    /** 행 고유키 필드 (default: 'id') */
    rowIdKey?: string | ((row: T) => string);
    /** 행 밀도 */
    density?: 'compact' | 'normal' | 'comfortable';
    /** 체크박스 선택 */
    selectable?: boolean;
    selectedIds?: string[];
    onSelectionChange?: (ids: string[]) => void;
    /** 정렬 */
    sortBy?: string;
    sortDirection?: 'asc' | 'desc' | null;
    onSortChange?: (col: string, dir: 'asc' | 'desc') => void;
    /** 로딩 (skeleton) */
    loading?: boolean;
    /** 빈 상태 커스텀(주면 emptyVariant 무시) */
    emptyState?: ReactNode;
    /** 빈 상태 종류 — empty(데이터 없음) | noResult(검색 결과 없음). default 'empty' */
    emptyVariant?: 'empty' | 'noResult';
    /** 행 클릭 */
    onRowClick?: (row: T, index: number) => void;
    /** 툴바 좌측 "총 N개" — 주면 툴바 표시 */
    totalCount?: number;
    /** 툴바 우측 엑셀 다운로드 버튼 핸들러 — 주면 버튼 표시 */
    onExcelDownload?: () => void;
    /** 엑셀 버튼 라벨 (default '엑셀 다운로드') */
    excelLabel?: string;
    className?: string;
    'data-testid'?: string;
    /** 행별 data-testid 생성 함수 */
    rowTestId?: (row: T, id: string) => string;
}
//# sourceMappingURL=Table.types.d.ts.map