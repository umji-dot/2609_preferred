import { CSSProperties, ElementType, ReactNode } from 'react';
import { SpacingToken } from './layout.types';
/**
 * Box — 모든 레이아웃의 기본 프리미티브.
 *
 * - 여백(padding/margin/gap)은 spacing 토큰 사용
 * - 크기(width/height)는 디자인 토큰이 없으므로 수치 직접 입력
 *   - `number` 는 px 로 해석 (`<Box width={64} />`)
 *   - `string` 은 그대로 CSS (`<Box width="50%" />`, `<Box height="100vh" />`)
 */
export interface BoxProps {
    as?: ElementType;
    children?: ReactNode;
    className?: string;
    style?: CSSProperties;
    id?: string;
    'data-testid'?: string;
    /** 사방 padding. paddingX/Y/Top/Bottom/Left/Right 와 동시 사용 시 더 구체적인 쪽이 우선. */
    padding?: SpacingToken;
    /** 좌우 padding. */
    paddingX?: SpacingToken;
    /** 상하 padding. */
    paddingY?: SpacingToken;
    paddingTop?: SpacingToken;
    paddingBottom?: SpacingToken;
    paddingLeft?: SpacingToken;
    paddingRight?: SpacingToken;
    margin?: SpacingToken;
    marginX?: SpacingToken;
    marginY?: SpacingToken;
    marginTop?: SpacingToken;
    marginBottom?: SpacingToken;
    marginLeft?: SpacingToken;
    marginRight?: SpacingToken;
    /** 픽셀(number) 또는 CSS 문자열(`'100%'`, `'50vh'`, `'auto'` 등). */
    width?: number | string;
    height?: number | string;
    minWidth?: number | string;
    maxWidth?: number | string;
    minHeight?: number | string;
    maxHeight?: number | string;
    full?: boolean;
    fullHeight?: boolean;
    grow?: boolean;
    onClick?: (e: React.MouseEvent<HTMLElement>) => void;
}
//# sourceMappingURL=Box.types.d.ts.map