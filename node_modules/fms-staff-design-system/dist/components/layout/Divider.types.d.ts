import { HTMLAttributes } from 'react';
import { SpacingToken } from './layout.types';
export type DividerOrientation = 'horizontal' | 'vertical';
/**
 * Divider — 구분선.
 *
 * - horizontal: width 100%, height 1px (위·아래 spacing)
 * - vertical: height stretch, width 1px (좌·우 spacing)
 * - 색상은 `bg-gray-200`. 변경 필요시 className 으로 override
 */
export interface DividerProps extends Omit<HTMLAttributes<HTMLDivElement>, 'role'> {
    orientation?: DividerOrientation;
    /** 방향에 맞는 축으로 margin 적용 (horizontal=my, vertical=mx) */
    spacing?: SpacingToken;
}
//# sourceMappingURL=Divider.types.d.ts.map