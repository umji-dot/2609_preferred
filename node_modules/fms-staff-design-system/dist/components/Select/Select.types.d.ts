/**
 * Figma Source: 03_Components > Select
 * props 는 staff Select 와 동일하게 유지 → drop-in 대체.
 */
export interface SelectOption {
    value: string;
    label: string;
    /** 옵션 2번째 줄 보조 정보 (예: 차량 검색결과의 제조사·모델·업체·지점) */
    description?: string;
    disabled?: boolean;
}
export interface SelectProps {
    label?: string;
    options: SelectOption[];
    placeholder?: string;
    value?: string | string[];
    onChange?: (value: string | string[]) => void;
    searchable?: boolean;
    /**
     * searchable 입력 변경 시 부모에 알림 (server-side 검색 트리거용).
     * 미지정 시 client-side filter(label.includes).
     */
    onSearchChange?: (search: string) => void;
    /** 드롭다운 끝까지 스크롤 시 호출 — 다음 페이지 fetch(무한스크롤). */
    onScrollEnd?: () => void;
    /** 추가 페이지 존재 여부 — false 면 onScrollEnd/로딩 표시 안 함. */
    hasMore?: boolean;
    /** 다음 페이지 로딩 중 — 하단 표시 + 중복 호출 방지. */
    loadingMore?: boolean;
    multiple?: boolean;
    status?: 'default' | 'error' | 'success';
    errorMessage?: string;
    successMessage?: string;
    helperText?: string;
    required?: boolean;
    readOnly?: boolean;
    disabled?: boolean;
    fullWidth?: boolean;
    id?: string;
    className?: string;
    /** E2E 테스트 ID — 트리거(combobox) 노드에 부착. */
    'data-testid'?: string;
}
//# sourceMappingURL=Select.types.d.ts.map