import { InputHTMLAttributes, ReactNode } from 'react';
/**
 * Figma Source: 03_Components > InputField
 * 컴포넌트명은 Figma 기준(InputField). props 는 staff Input 과 동일하게 유지 → drop-in 대체 가능.
 *
 * States(Figma): Default · Hover · Focus · Filled · Error · Success · Disabled · ReadOnly
 *   → status(default/error/success) + disabled/readOnly + hover/focus(CSS) 로 표현
 */
export interface InputFieldProps extends Omit<InputHTMLAttributes<HTMLInputElement>, 'size' | 'onChange'> {
    /** Field label (always visible). string 또는 ReactNode */
    label?: ReactNode;
    /** Validation status */
    status?: 'default' | 'error' | 'success';
    /** Red error text */
    errorMessage?: string;
    /** Green success text */
    successMessage?: string;
    /** Gray helper text */
    helperText?: string;
    /** Show * indicator */
    required?: boolean;
    /** View but not edit */
    readOnly?: boolean;
    /** readOnly 인데 회색(muted) 대신 일반 인풋색 유지 — 클릭으로 picker 여는 필드 등 */
    readOnlyPlain?: boolean;
    /** Cannot interact */
    disabled?: boolean;
    fullWidth?: boolean;
    /** Custom icon (right side) */
    icon?: ReactNode;
    onChange?: (value: string) => void;
}
//# sourceMappingURL=InputField.types.d.ts.map