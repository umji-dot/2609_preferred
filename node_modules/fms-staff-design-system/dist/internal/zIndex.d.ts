/**
 * DS 내부 Portal 구현용 z-index 상수.
 *
 * 비공개 — public barrel 로 export 하지 않는다.
 * Modal / Toast / Tooltip / Popover / Dropdown 등 Portal 컴포넌트 구현에서만 사용.
 *
 * 소비 앱은 각자의 z-index(0~999)를 쓰고, DS Portal 은 서로 간섭하지 않도록
 * 1000~1599 범위의 큰 값을 사용한다. (design-publish R8 규약과 일치)
 */
export declare const IDS_Z_INDEX: {
    readonly DROPDOWN: 1000;
    readonly OVERLAY: 1100;
    readonly MODAL: 1200;
    readonly POPOVER: 1300;
    readonly TOAST: 1400;
    readonly TOOLTIP: 1500;
};
//# sourceMappingURL=zIndex.d.ts.map