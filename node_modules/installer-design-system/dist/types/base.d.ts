import { CSSProperties, ReactNode } from 'react';
/**
 * 모든 컴포넌트에 공통으로 부여되는 기본 props.
 *
 * - `id`, `className`, `style`: 호출 측이 개별 조정할 수 있는 escape hatch
 * - `data-testid`: 테스트 용도
 * - `children`: 콘텐츠 있는 컴포넌트에서 선택적으로 사용
 */
export interface BaseProps {
    id?: string;
    className?: string;
    style?: CSSProperties;
    'data-testid'?: string;
    children?: ReactNode;
}
//# sourceMappingURL=base.d.ts.map