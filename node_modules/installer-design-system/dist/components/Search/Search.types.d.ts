import { ChangeEvent, InputHTMLAttributes, ReactNode } from 'react';
import { BaseProps } from '../../types';
export interface SearchProps extends BaseProps, Omit<InputHTMLAttributes<HTMLInputElement>, 'onChange' | 'size' | 'type'> {
    label?: ReactNode;
    required?: boolean;
    onChange?: (e: ChangeEvent<HTMLInputElement>) => void;
    /**
     * 입력값을 지우는 콜백. 전달 시 값이 있을 때 우측에 X(cancel) 버튼이 나타난다.
     * disabled 상태에서는 노출되지 않는다.
     */
    onClear?: () => void;
}
//# sourceMappingURL=Search.types.d.ts.map