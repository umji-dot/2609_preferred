import { HTMLAttributes, ReactNode } from 'react';
import { BaseProps } from '../../types';
export type MemoVariant = 'default' | 'wait' | 'error';
export interface MemoProps extends BaseProps, Omit<HTMLAttributes<HTMLDivElement>, 'title'> {
    variant?: MemoVariant;
    /** Material Symbols 아이콘명. 미지정 시 variant 기본값 사용. */
    icon?: string;
    title: ReactNode;
    children: ReactNode;
}
//# sourceMappingURL=Memo.types.d.ts.map