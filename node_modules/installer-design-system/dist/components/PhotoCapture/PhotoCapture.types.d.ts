import { HTMLAttributes, ReactNode } from 'react';
import { BaseProps } from '../../types';
export interface PhotoCaptureProps extends BaseProps, Omit<HTMLAttributes<HTMLDivElement>, 'onClick'> {
    /** 컨테이너 가로. number=px, string=CSS 값('50%' 등). 미지정+`full` 미지정 시 자체 폭 없음. */
    width?: number | string;
    /** true 일 때 `w-full` 적용 → 부모 폭 100%. `width` 와 동시 지정 시 `width` 가 inline style 로 이김. */
    full?: boolean;
    /** 사진 URL. 지정 시 사진 표시 + "재촬영" 버튼. */
    src?: string;
    /** 가운데 텍스트 라벨 (예: "차량 번호판") */
    label?: ReactNode;
    /** 버튼 텍스트. 미지정 시 src 유무로 "촬영"/"재촬영" 자동. */
    buttonLabel?: ReactNode;
    onCapture?: () => void;
}
//# sourceMappingURL=PhotoCapture.types.d.ts.map