import { HTMLAttributes, ReactNode } from 'react';
import { BaseProps } from '../../types';
export type DetailListVariant = 'pairs' | 'single';
/**
 * DetailList — 작업 상세 리스트 카드 (Figma node 28:918).
 *
 * - bg white, border neutral-200, radius 12, padding 16
 * - 행 사이 자동 Divider 삽입, 행 간 gap 12 (Figma 10 → 토큰 라운딩)
 * - variant 'pairs' (기본): label+value 2열
 * - variant 'single': 단일 열 텍스트
 */
export interface DetailListProps extends BaseProps, HTMLAttributes<HTMLDivElement> {
    variant?: DetailListVariant;
}
/**
 * DetailRow — DetailList 내부 행.
 *
 * - 부모 'pairs' 일 때: `label` 왼쪽(regular 16 neutral-600), children 오른쪽(medium 16 black, 우정렬)
 * - 부모 'single' 일 때: children 만 medium 16 black 출력, `label` 무시
 */
export interface DetailRowProps extends BaseProps, HTMLAttributes<HTMLDivElement> {
    label?: ReactNode;
}
//# sourceMappingURL=DetailList.types.d.ts.map