import { ToastEntry, ToastOptions } from './Toast.types';
interface ToastState {
    stack: ToastEntry[];
    push: (entry: Omit<ToastEntry, 'id'>) => string;
    /** id 항목을 exit 단계로 마크 → EXIT_DURATION 후 실제 제거. */
    remove: (id: string) => void;
}
declare const useToastStore: import('zustand').UseBoundStore<import('zustand').StoreApi<ToastState>>;
export { useToastStore };
export declare const toast: {
    success: (options: ToastOptions) => string;
    error: (options: ToastOptions) => string;
    /** 특정 toast 수동 dismiss (exit 애니메이션 거쳐 사라짐). */
    dismiss: (id: string) => void;
};
//# sourceMappingURL=toastStore.d.ts.map