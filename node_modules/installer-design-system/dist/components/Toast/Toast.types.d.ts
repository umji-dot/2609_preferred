import { HTMLAttributes, ReactNode } from 'react';
export type ToastTone = 'success' | 'error';
export type ToastPosition = 'top-center' | 'center' | 'bottom-center';
export interface ToastProps extends Omit<HTMLAttributes<HTMLDivElement>, 'title'> {
    variant?: ToastTone;
    title: ReactNode;
    description?: ReactNode;
}
/** toast.success / toast.error 호출 시 받는 옵션. */
export interface ToastOptions {
    title: ReactNode;
    description?: ReactNode;
    /** ms 단위 자동 dismiss. 기본 3000. */
    duration?: number;
    /** 표시 위치. 기본 'top-center'. */
    position?: ToastPosition;
}
/** Toaster 내부 store 가 보관하는 항목. */
export interface ToastEntry {
    id: string;
    variant: ToastTone;
    title: ReactNode;
    description?: ReactNode;
    duration: number;
    position: ToastPosition;
    /** true 면 exit 애니메이션 단계. 200ms 후 실제 unmount. */
    exiting?: boolean;
}
//# sourceMappingURL=Toast.types.d.ts.map