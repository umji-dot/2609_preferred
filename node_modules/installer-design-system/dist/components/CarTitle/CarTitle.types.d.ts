import { HTMLAttributes, ReactNode } from 'react';
import { BaseProps } from '../../types';
/**
 * CarTitle — 차량명 + 번호판 카드 (Figma node 67:1375 `car-title`).
 *
 * - bg white, border neutral-200, radius lg-12, padding 16
 * - 2행 stack, gap 8
 *   - carModel: 16 Regular black
 *   - plateNumber: 24 Bold black
 * - 기본 width 100% (`full` true). `width` 로 고정 폭 지정 가능
 */
export interface CarTitleProps extends BaseProps, HTMLAttributes<HTMLDivElement> {
    carModel: ReactNode;
    plateNumber: ReactNode;
    /** 고정 폭. 지정 시 inline style 로 적용되며 `full` 보다 우선. */
    width?: number | string;
    /** true 일 때 `w-full` 적용 → 부모 폭 100%. 기본 true. */
    full?: boolean;
}
//# sourceMappingURL=CarTitle.types.d.ts.map