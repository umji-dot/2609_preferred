import { BoxProps } from '../Box/Box.types';
import { SpacingToken } from '../../../types';
/**
 * Grid — CSS Grid 레이아웃 프리미티브.
 *
 * - columns / rows
 *   - `number` : `repeat(N, minmax(0, 1fr))` 균등 분할
 *   - `string` : `gridTemplateColumns`/`Rows` 에 그대로 적용 (`'1fr 2fr 1fr'`, `'200px auto 1fr'` 등)
 * - gap 은 IDS spacing 토큰만 (row/column 별도 지정 가능)
 * - areas 는 행 단위 문자열 배열 → `grid-template-areas` 로 변환
 * - center 는 모든 cell 안 콘텐츠를 가운데 정렬 (`place-items-center`)
 */
export interface GridProps extends BoxProps {
    columns?: number | string;
    rows?: number | string;
    gap?: SpacingToken;
    rowGap?: SpacingToken;
    columnGap?: SpacingToken;
    areas?: string[];
    /** 각 cell 안에서 아이템을 가운데 정렬 (`place-items-center`). */
    center?: boolean;
}
//# sourceMappingURL=Grid.types.d.ts.map