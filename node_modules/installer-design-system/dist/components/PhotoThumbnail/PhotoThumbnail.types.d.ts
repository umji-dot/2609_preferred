import { HTMLAttributes } from 'react';
import { BaseProps } from '../../types';
export interface PhotoThumbnailProps extends BaseProps, HTMLAttributes<HTMLDivElement> {
    /** 컨테이너 가로. number=px, string=CSS 값('50%' 등). 미지정+`full` 미지정 시 자체 폭 없음. */
    width?: number | string;
    /** true 일 때 `w-full` 적용 → 부모 폭 100%. `width` 와 동시 지정 시 `width` 가 inline style 로 이김. */
    full?: boolean;
    /** 사진 URL. 필수. */
    src: string;
    onDelete?: () => void;
}
//# sourceMappingURL=PhotoThumbnail.types.d.ts.map