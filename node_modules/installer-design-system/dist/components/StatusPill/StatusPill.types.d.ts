import { HTMLAttributes, ReactNode } from 'react';
import { BaseProps } from '../../types';
export type StatusPillStatus = 'completed' | 'in-progress' | 'pending' | 'failed' | 'operated';
/**
 * StatusPill — 상태 라벨 + 아이콘 한 쌍 (Figma node 28:1301).
 *
 * - text 15 Bold, line-height 1.5
 * - text 와 icon 사이 gap 4 (Figma 2 → 토큰 라운딩)
 * - 5 상태:
 *   - completed: text-success + check_circle (filled)
 *   - in-progress: text-primary, 아이콘 없음
 *   - pending: text-neutral-500 + clock_loader_10
 *   - failed: text-danger + cancel (filled)
 *   - operated: text-neutral-600 + check_circle (filled)  // Figma "운영 완료"
 */
export interface StatusPillProps extends BaseProps, HTMLAttributes<HTMLSpanElement> {
    status: StatusPillStatus;
    /** 상태 텍스트. 미지정 시 status별 기본 라벨 사용 (완료/진행중.../대기/실패). */
    children?: ReactNode;
}
//# sourceMappingURL=StatusPill.types.d.ts.map