import { HTMLAttributes, ReactNode } from 'react';
import { BaseProps } from '../../types';
/**
 * JobCard 완료 모드 정보.
 *
 * `JobCard.completion` 으로 전달. 객체 prop 으로 묶여 있어 모드 분기·필드 관계가 명시적.
 */
export interface JobCardCompletion {
    /** 완료/불가 일시 (예: '2026-03-15 12:51'). */
    date: string;
    /** status 종류. */
    status: 'completed' | 'failed';
    /**
     * 우측 StatusPill 라벨 override.
     * 미지정 시 JobCard 도메인 기본:
     *  - completed → '완료'
     *  - failed → '작업 불가'
     */
    label?: ReactNode;
    /**
     * date prefix override.
     * 미지정 시:
     *  - completed → '작업 완료일'
     *  - failed → '작업 불가일'
     */
    datePrefix?: string;
}
export interface JobCardProps extends BaseProps, HTMLAttributes<HTMLDivElement> {
    carModel: string;
    plateNumber: string;
    branch: string;
    /** 작업 요청일 (tasks 모드 필수, completion 모드에서는 무시). */
    date?: string;
    /** tasks 모드 전용. */
    address?: string;
    /** tasks 모드 전용. */
    phone?: string;
    badges?: ReactNode;
    /** 있으면 completion 모드 (이력) — address/phone 숨김, completion.date 표시, 우측 StatusPill. */
    completion?: JobCardCompletion;
}
//# sourceMappingURL=JobCard.types.d.ts.map