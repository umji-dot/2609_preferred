import { HTMLAttributes, ReactNode } from 'react';
import { BaseProps } from '../../types';
export interface InfoProps extends BaseProps, Omit<HTMLAttributes<HTMLDivElement>, 'title'> {
    /** Material Symbols 아이콘명. 기본 "info". */
    icon?: string;
    title: ReactNode;
    children: ReactNode;
}
//# sourceMappingURL=Info.types.d.ts.map