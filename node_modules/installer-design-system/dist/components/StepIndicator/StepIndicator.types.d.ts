import { HTMLAttributes } from 'react';
import { BaseProps } from '../../types';
export interface StepIndicatorProps extends BaseProps, HTMLAttributes<HTMLDivElement> {
    total: number;
    current: number;
    /** 미지정 시 각 segment가 균등 분배(flex-1). 숫자 지정 시 해당 px 고정 폭. */
    segmentWidth?: number;
}
//# sourceMappingURL=StepIndicator.types.d.ts.map