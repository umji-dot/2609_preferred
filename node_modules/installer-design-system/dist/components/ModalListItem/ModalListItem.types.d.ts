import { HTMLAttributes, ReactNode } from 'react';
import { BaseProps } from '../../types';
/**
 * ModalListItem — 모달 내 단일 선택 옵션 행 (Figma `ModalList` 80:1447).
 *
 * - bg white, border 1px, radius md-8, padding 16
 * - default: border neutral-400
 * - selected: border primary + 우측 check_circle 24
 * - 타이틀: 17 Bold black
 * - 기본 width 100% (모달 내에서 stack 으로 가득 채움)
 */
export interface ModalListItemProps extends BaseProps, Omit<HTMLAttributes<HTMLButtonElement>, 'type'> {
    /** 선택 상태 */
    selected?: boolean;
    /** 비활성 */
    disabled?: boolean;
    /** 행 라벨 */
    children: ReactNode;
}
//# sourceMappingURL=ModalListItem.types.d.ts.map