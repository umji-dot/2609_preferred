import { HTMLAttributes, ReactNode } from 'react';
import { BaseProps } from '../../types';
export type VerificationStatus = 'pending' | 'in-progress' | 'completed' | 'failed';
export interface VerificationItemProps extends BaseProps, Omit<HTMLAttributes<HTMLDivElement>, 'title'> {
    title: ReactNode;
    status: VerificationStatus;
    pendingLabel?: ReactNode;
    inProgressLabel?: ReactNode;
    completedLabel?: ReactNode;
    failedLabel?: ReactNode;
}
//# sourceMappingURL=VerificationItem.types.d.ts.map