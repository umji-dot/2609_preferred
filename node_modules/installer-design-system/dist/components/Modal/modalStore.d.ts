import { AlertOptions, ConfirmOptions, ModalEntry } from './Modal.types';
interface ModalState {
    stack: ModalEntry[];
    push: (entry: Omit<ModalEntry, 'id'>) => string;
    closeById: (id: string, value?: boolean) => void;
}
export declare const useModalStore: import('zustand').UseBoundStore<import('zustand').StoreApi<ModalState>>;
export declare const modal: {
    alert: (options: AlertOptions) => Promise<void>;
    confirm: (options: ConfirmOptions) => Promise<boolean>;
};
export {};
//# sourceMappingURL=modalStore.d.ts.map