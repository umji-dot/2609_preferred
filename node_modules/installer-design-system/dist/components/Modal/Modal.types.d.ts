import { ReactNode } from 'react';
export interface StepIndicatorOption {
    total: number;
    current: number;
}
export interface AlertOptions {
    title: ReactNode;
    /** 타이틀 바로 아래 본문 텍스트 (18 Regular). 줄바꿈은 \n. */
    subtitle?: ReactNode;
    /** subtitle 아래 자유 ReactNode 영역 (Info 컴포넌트 등). */
    content?: ReactNode;
    /** 타이틀 위 progress 표시. infoMessage 와 동시에 쓰지 않음. */
    stepIndicator?: StepIndicatorOption;
    /** 타이틀 위 작은 라벨 텍스트. stepIndicator 와 동시에 쓰지 않음. */
    infoMessage?: ReactNode;
    confirmLabel?: string;
    tone?: 'primary' | 'danger';
}
export interface ConfirmOptions extends AlertOptions {
    cancelLabel?: string;
    /** cancel 버튼 색 (좌측). 기본 'secondary'(회색). 'danger' 시 빨강. */
    cancelTone?: 'secondary' | 'danger';
}
export type ModalEntry = {
    id: string;
    kind: 'alert';
    options: AlertOptions;
    resolve: () => void;
} | {
    id: string;
    kind: 'confirm';
    options: ConfirmOptions;
    resolve: (value: boolean) => void;
};
//# sourceMappingURL=Modal.types.d.ts.map