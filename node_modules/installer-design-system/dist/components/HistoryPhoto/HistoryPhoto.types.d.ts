import { HTMLAttributes, ReactNode } from 'react';
import { BaseProps } from '../../types';
/**
 * HistoryPhoto — 이력 화면용 증빙 사진 카드 (Figma node 0:434, history-photo variant).
 *
 * - bg-black + border neutral-200 + radius 12
 * - 사진 50% opacity 로 깔리고 중앙에 항목명 라벨 (white, micro-14 Regular)
 * - 우상단 flip_camera_ios 아이콘 (재촬영 액션)
 * - 기본 사이즈 106x106 (Figma 진리값)
 */
export interface HistoryPhotoProps extends BaseProps, HTMLAttributes<HTMLDivElement> {
    /** 컨테이너 가로. number=px (기본 106), string=CSS 값. */
    width?: number | string;
    /** true 일 때 w-full → 부모 폭 100% (width 무시). */
    full?: boolean;
    /** 사진 URL. 필수. */
    src: string;
    /** 중앙 라벨 (예: "단말기 고정"). 항목명. */
    label: ReactNode;
    /** 재촬영 액션. 미지정 시 우상단 flip 아이콘 미표시. */
    onRetake?: () => void;
}
//# sourceMappingURL=HistoryPhoto.types.d.ts.map