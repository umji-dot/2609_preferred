import { ButtonHTMLAttributes, ReactNode } from 'react';
/**
 * Figma 정의의 button type 에 매핑된다.
 * - primary, secondary, outline (tone=default)
 * - danger (= primary tone=danger)
 * - success (= primary tone=success)
 */
export type ButtonVariant = 'primary' | 'secondary' | 'outline' | 'danger' | 'success';
/**
 * Figma 정의: ButtonLage(56) / ButtonMedium(40).
 */
export type ButtonSize = 'md' | 'lg';
export interface ButtonProps extends Omit<ButtonHTMLAttributes<HTMLButtonElement>, 'type'> {
    variant?: ButtonVariant;
    size?: ButtonSize;
    loading?: boolean;
    type?: 'button' | 'submit' | 'reset';
    iconLeft?: ReactNode;
    iconRight?: ReactNode;
}
//# sourceMappingURL=Button.types.d.ts.map