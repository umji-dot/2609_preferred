import { HTMLAttributes, ReactNode } from 'react';
import { BaseProps } from '../../types';
/**
 * NoImage — 등록된 이미지가 없을 때 자리표시 (Figma node 67:1381 `no-img`).
 *
 * - bg neutral-50, radius lg-12, height 240
 * - 기본 width 100% (보통 부모 폭에 맞춰 들어감). `width` 로 고정 폭 지정 가능
 * - 중앙: photo 아이콘 48 + 텍스트 17 Regular neutral-700
 */
export interface NoImageProps extends BaseProps, HTMLAttributes<HTMLDivElement> {
    /** 표시 텍스트. 미지정 시 "등록된 이미지 없음". */
    label?: ReactNode;
    /** 고정 폭. 지정 시 inline style 로 적용되며 `full` 보다 우선. */
    width?: number | string;
    /** true 일 때 `w-full` 적용 → 부모 폭 100%. 기본 true. */
    full?: boolean;
}
//# sourceMappingURL=NoImage.types.d.ts.map