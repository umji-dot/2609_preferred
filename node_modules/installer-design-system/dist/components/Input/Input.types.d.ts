import { ChangeEvent, InputHTMLAttributes, ReactNode } from 'react';
import { BaseProps } from '../../types';
export interface InputProps extends BaseProps, Omit<InputHTMLAttributes<HTMLInputElement>, 'onChange' | 'size'> {
    label?: ReactNode;
    required?: boolean;
    error?: boolean;
    success?: boolean;
    onChange?: (e: ChangeEvent<HTMLInputElement>) => void;
    /**
     * 입력값을 지우는 콜백. 전달 시 값이 있을 때 우측에 X(cancel) 버튼이 나타난다.
     * 단, disabled/error/success 상태에서는 노출되지 않는다.
     */
    onClear?: () => void;
}
//# sourceMappingURL=Input.types.d.ts.map